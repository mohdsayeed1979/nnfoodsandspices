# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — NEVER commit real secrets (anon key, service-role key, admin password)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# bulk-import-images.mjs output — downloaded/processed images, backups and
# per-run reports. Never committed: large binaries + can contain the full
# pre-change DB snapshot. See scripts/bulk-import-images.mjs.
/scripts/output/

# typescript
*.tsbuildinfo
